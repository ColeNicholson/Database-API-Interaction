In this directory I have included:
This README.txt file.
Project 2 Report.docx which details each of the curl commands to use on the api.
PC_Parts.sql & PC_Parts_Test_Queries.sql to allow for the rebuilding of the database if necessary.
controllers/*Controller.js which are the database controllers for the api. The BuildController.js contains the function which references a view.
routes/*.js which hold the required routes for the urls.
api.js which the node command should be run on to intialize the instance of server.